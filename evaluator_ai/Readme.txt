To create a virtual Environment run the below codes:

py -m venv venv 
.\venv\Scripts\Activate

Run the below code to install all the libraries:
pip install -r requirements.txt

To run Arabic version run below code:
python V5.py

To run English version run below code:
python V6.py
